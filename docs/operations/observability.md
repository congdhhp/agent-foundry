# Observability

## Required telemetry

```text
task latency
tool latency
model latency
tool failure rate
policy deny rate
approval rate
approval rejection rate
evidence coverage
eval pass rate
cost per task
context size
memory load size
hook execution latency
snapshot resolution latency
```
