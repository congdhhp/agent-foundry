# SLOs and Budgets

## Recommended MVP budgets

```text
max tool retries: 2
max verifier retries: 2
max approval wait: configurable
max context budget: configurable per model
max task cost: configurable per agent
max task duration: configurable per workflow
```

## Metrics

```text
p50/p95 task latency
p50/p95 tool latency
policy decision latency
evidence coverage
eval pass rate
cost per successful run
```
